Denne folder indeholder wsdl og servicebeskrivelser for følgende services:
- RemedyDokumentOpret v.1.0
- RemedySagOpret v.1.1

(De nævnte services skal ikke implementeres fra bunden men etableres via en XSLT transformation på bussen)

Lasse Buck